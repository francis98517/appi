{
  "name": "meu-app-json-server",
  "version": "1.0.0",
  "scripts": {
    "start": "json-server --watch db.json --port $PORT"
  },
  "engines": {
    "node": "16.x"  
  },
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
